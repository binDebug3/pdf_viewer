The next numbered phase in the plan is Phase 5: Undo/Redo and Session Reliability.

That said, the implementation is still in the middle of Phase 4 in practical terms. The new split 
mode exists, but the original Phase 4 checklist also calls for richer inspector controls, 
reset/edit behavior, and stronger preview-to-export alignment. So the roadmap is:

Finish the remaining Phase 4 split workflow polish.
Move into Phase 5 for command-based undo/redo, dirty tracking, and unsaved-changes protection.
Then Phase 6 for full Save As and export reliability.
If you want the most sensible next implementation step right now, it’s Phase 5 groundwork: 
introduce a command/history layer so split edits, reorder, delete, and duplicate can become 
reversible instead of staying one-way.